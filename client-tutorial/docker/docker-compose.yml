version: '3.9'

services:
 mysql:
    profiles: ["deployment"]
    image: mysql:8.0.15
    ports:
        - "3306:3306"
    command: --init-file  /Users/henryhbk/gitclones/jomis_lfh/database scripts/create_demo_database.sql
    volumes:
       - ./database-scripts:/docker-entrypoint-initdb.d
    environment:
        MYSQL_ROOT_USER: root
        MYSQL_ROOT_PASSWORD: secret
        MYSQL_DATABASE: kafkaFhirDemoDb
        MYSQL_USER: lfhuser
        MYSQL_PASSWORD: changePassword
